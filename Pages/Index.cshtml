@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="row">
    <div class="col">
        <h1 class="display-4">Speak with your data!</h1>
    </div>
</div>
<div class="row">
    <div class="col">
        <p>This chatboot about questions about the data in your company</p>
        <form asp-page="Index" class="form">
            <div class="form-group mb-3">
                <label for="name">Question</label>
                <input type="text" asp-for="@Model.Question" class="form-control" />
            </div>
            <input type="submit" class="btn btn-primary" />
        </form>
        <hr />
        <p>@Model.ResponseContent</p>
    </div>
</div>

